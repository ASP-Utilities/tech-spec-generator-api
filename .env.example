# Server Configuration
PORT=3001
NODE_ENV=development

# CORS - Frontend URL
FRONTEND_URL=http://localhost:5173

# Logging Configuration
# Service name for structured logging
SERVICE_NAME=tech-spec-generator-api
# Log level (trace, debug, info, warn, error, fatal)
LOG_LEVEL=debug

# Database Configuration
# Local Development: Use direct PostgreSQL connection
# Production: Will be retrieved from Google Secret Manager
DATABASE_URL=postgresql://username:password@localhost:5432/your_database_name

# Google Cloud Platform Configuration
# Required for both local development and production
GCP_PROJECT_ID=your-gcp-project-id-here

# Local Development Only: Path to service account key file
# In production, Workload Identity handles authentication automatically
GOOGLE_APPLICATION_CREDENTIALS=./keys/service-account.json

# Production Only: Cloud SQL instance connection name
# Format: project:region:instance-name
# CLOUD_SQL_CONNECTION_NAME=your-project:us-central1:your-instance-name
